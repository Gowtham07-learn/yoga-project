# Frontend environment variables (Vite) - DO NOT COMMIT SECRETS
# Rename this to .env.local for local overrides and do NOT commit that file.

# Example API base URL
VITE_API_URL=https://api.example.com

# Path or URL to MediaPipe models (if used)
VITE_MEDIAPIPE_MODEL_PATH=

# App info
VITE_APP_NAME=YogaPoseApp

# Feature flags
VITE_ENABLE_DEMO=false

# Add your own keys here, prefixed with VITE_ so Vite exposes them to client code
# e.g. VITE_SOME_KEY=your_value
